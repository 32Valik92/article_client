const baseURLServer = "http://localhost:4444";

const urls = {
   auth: {
      login: "/auth/login",
      register: "/auth/register",
      me: "/auth/me"
   }
};

export {
   baseURLServer,
   urls
};